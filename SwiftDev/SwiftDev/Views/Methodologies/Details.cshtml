@model SwiftDev.Models.Methodology

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Methodology</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MethodologyName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MethodologyName)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.MethodologyID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
